<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.tecnm.misantla.listviewapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListViewApp">
        <activity android:name=".SandiaActivity"></activity>
        <activity android:name=".FresaActivity" />
        <activity android:name=".NaranjaActivity" />
        <activity android:name=".MangoActivity" />
        <activity android:name=".KiwiActivity" />
        <activity android:name=".UvaActivity" />
        <activity android:name=".LimonActivity" />
        <activity android:name=".PlatanoActivity" />
        <activity android:name=".AppleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>